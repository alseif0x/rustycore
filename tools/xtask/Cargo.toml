[package]
name = "xtask"
version = "0.1.0"
edition = "2024"
publish = false
description = "Structure automation for the RustyCore workspace: audit, layer edges, banned dependencies, agent context and scope."

[dependencies]

[lints]
workspace = true
