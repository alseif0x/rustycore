[package]
name = "wow-persistence"
version.workspace = true
edition.workspace = true
license.workspace = true

# Intentionally dependency-free. This crate owns the SQLx-free lifecycle
# capability; the MariaDB/SQLx adapter lives in `wow-database`.
[dependencies]
