[package]
name = "wow-persistence"
version.workspace = true
edition.workspace = true
license.workspace = true

# Intentionally dependency-free. This crate owns SQLx-free persistence
# capabilities; their MariaDB/SQLx adapters live in `wow-database`.
[dependencies]
